# 0.1.0.beta.8 업데이트 (26.06.21)

## patch 추가

http method 중 하나인 patch가 추가되었습니다.

```ts
const denchInstacne = dench("url");
const denchInstance.patch<apitype>("api");

```

## head 추가

http method 중 하나인 head가 추가되었습니다.

```ts
const denchInstance = dench("url");
const denchInstance.head("a");


## 버그 수정

- params를 적용하여 쿼리 파라미터를 추가하면 fetch가 잘 동작하지 않는 문제를 해결했습니다.
- params의 parameter가 제대로 적용되지 않는 문제를 해결했습니다.


## 최적화

- 번들 파일의 크기를 일부 줄였습니다 (13.9kb -> 11.4kb)